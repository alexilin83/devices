import { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
    token: {
    },
    components: {
        Button: {
            defaultBg: "#e2e8f0",
            defaultColor: '#64748b',
        },
        Menu: {
            padding: 10
        }
    }
};

export default theme;